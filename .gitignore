# Brand guide assets (huge PDF, not source) - logos already copied into
# frontend/public/images/logo/ where they're actually used by the app.
identidade/

# Old static landing page (its own nested git repo) - reference content only,
# already rebuilt as frontend/src/pages/Landing/ and assets copied into
# frontend/public/images/landing/.
landingpage/

# Local MCP server config (shadcn mcp init)
.mcp.json

# Local shadcn MCP install artifacts at repo root (not part of the app)
/node_modules/
/package.json
/package-lock.json
